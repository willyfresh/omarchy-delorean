-- Delorean overlay — niri-like side-scrolling windows by default.
-- Super+Ctrl+L still toggles this workspace between scrolling and dwindle.

hl.config({
  general = {
    layout = "scrolling",
  },
})
